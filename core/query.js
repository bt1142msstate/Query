/**
 * Compatibility shim.
 * Query builder shell behavior now lives in ui/queryBuilderShell.js,
 * table rendering lives in ui/queryTableView.js,
 * and table-name input behavior lives in ui/tableNameInput.js.
 */
